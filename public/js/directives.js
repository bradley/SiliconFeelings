define(['angular', 'three'], function (angular) {
	'use strict';

  /* Services */

	angular.module('myApp.directives', [])
		.directive('ngWebgl', ['$rootScope', function ($rootScope) {
		  // https://github.com/winkerVSbecks/angularWebglDirective/blob/master/ngWebgl.js
		}]);
});
